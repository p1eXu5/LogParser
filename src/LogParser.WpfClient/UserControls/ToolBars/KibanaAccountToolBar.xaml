<ToolBar x:Class="LogParser.WpfClient.UserControls.ToolBars.KibanaAccountToolBar"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
         xmlns:local="clr-namespace:LogParser.WpfClient.UserControls.ToolBars"
         xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         d:DesignWidth="800"
         d:Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
         Style="{StaticResource st_LogParserToolBar}"
         mc:Ignorable="d"
         >
    <!--
    <ComboBox Margin="0,1,0,1"
              materialDesign:HintAssist.Hint="Kibana Account"
              IsEditable="False"
              IsTextSearchEnabled="False"
              ItemsSource="{Binding KibanaAccounts}"
              SelectedIndex="{Binding SelectedKibanaAccountIndex}"
              Style="{StaticResource st_ToolBarComboBox}"
              Text="{Binding SelectedKibanaBaseUri,
                             UpdateSourceTrigger=PropertyChanged,
                             Mode=OneWay}"
              />

    <TextBox Width="200"
             materialDesign:HintAssist.Hint="Login"
             Style="{StaticResource st_ToolBarTextBox}"
             Text="{Binding SelectedKibanaLogin,
                            UpdateSourceTrigger=PropertyChanged}"
             />

    <PasswordBox x:Name="m_Password"
                 Width="200"
                 materialDesign:HintAssist.Hint="Password"
                 materialDesign:PasswordBoxAssist.Password="{Binding SelectedKibanaPassword,
                                                                     UpdateSourceTrigger=PropertyChanged}"
                 Style="{StaticResource st_ToolBarPasswordBox}"
                 />
    -->
</ToolBar>
